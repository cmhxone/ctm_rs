refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "[{h({l})}][{d(%Y-%m-%d %H:%M:%S.%3f)}][{T}]{D([{M}:{L}])} - {m}{n}"

root:
  level: debug
  appenders:
    - stdout